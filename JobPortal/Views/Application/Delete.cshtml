@model DeleteApplicationViewModel
@{
	ViewBag.Title = "Delete Job Application";
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />

<div class="row justify-content-center">
	<div class="col-md-6">
		<div class="card">
			<div class="card-body">
				<h3 class="card-title text-center">Application Name: @Model.Name</h3>
				<h4 class="card-title text-center">Last Updated On: @Model.UpdatedOn</h4>
				<p class="text-danger mt-3">Are you sure you want to delete this job offer?</p>
			</div>
			<div class="card-footer">
				<form asp-action="ConfirmDelete">
					<input type="hidden" asp-for="@Model.Id" />
					<button type="submit" class="btn btn-danger">Confirm Delete</button>
					<a asp-action="Mine" class="btn btn-secondary">Cancel</a>
				</form>
			</div>
		</div>
	</div>
</div>
